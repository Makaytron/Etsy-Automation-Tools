'use strict';

// Source version only. Production userscripts receive this marker after the Ads pilot passes.
const MKUI_VERSION = '1.0.0';

if (typeof module !== 'undefined' && module.exports) {
    module.exports = { MKUI_VERSION };
}
