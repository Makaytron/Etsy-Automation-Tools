# Etsy Automation Tools v1.0.1 tag-only checkpoint

This signed tag was not published as a GitHub Release or sent to Greasy Fork. Final release QA found documentation mismatches, so the immutable checkpoint was superseded by `v1.0.2` instead of being moved or deleted.

## Highlights

- Verified every live homepage, support, Raw update/download, logo, privacy, issue, and GitHub API URL against the new canonical repository.
- Preserved legacy `@namespace` values solely to keep installed userscript identities stable; no network request uses the retired repository.
- Verified and retained the existing `@antifeature tracking` metadata for the documented privacy-preserving telemetry.
- Registered five Greasy Fork listings backed by their exact nested public Raw `main` paths.
- Added one release-only GitHub webhook for immediate refresh requests; Greasy Fork automatic synchronization remains enabled. Greasy Fork receives public release notifications but no GitHub credential or write access.
- Added a credential-free local validation command and repository secret-deny patterns.
- No Etsy reading, navigation, write, send, storage, or automation behavior changed in this patch.

## Installable scripts

- `Etsy-Sale-Campaign-Batch-Runner.user.js`
- `Makaytron-Etsy-Message-Assistant.user.js`
- `Makaytron-Etsy-Ads-Keyword-Manager.user.js`
- `Makaytron-Etsy-Listing-Analyzer.user.js`
- `Makaytron-Etsy-Keyword-Market-Analyzer.user.js`

See [DISTRIBUTION.md](../../DISTRIBUTION.md) for the corrected hosted-listing, update-routing, and credential boundary used by `v1.0.2`.
